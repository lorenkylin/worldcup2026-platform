# v0.7.0a "ModelBlend" 发布报告

> 发布日期: 2026-06-16
> 工时: ~2 小时(spec 30min + 代码 45min + 测试 15min + 文档 30min)
> 测试: **11/11 v0.7.0a 测试全绿 + 280 passed, 1 skipped 全套零回归**(实测 2026-06-16 11:36,259.50s)
> 策略: 选项 1(ModelBlend 起步 → Simulator → 赔率 API → 真 MarketBlend)

---

## 🎯 目标达成

把 v1 (Elo) + v3 (Glicko-2) 两套预测**等权平均**为第三套"blend"预测,降低单模型方差。

## 📦 改动清单(3 文件,~150 行)

| 文件 | 改动 | 行数 |
|---|---|---|
| `app/services/elo.py` | 新增 `predict_match_blend()` 纯函数 | +120 |
| `app/routers/elo.py` | 新增 `GET /api/elo/predict-blend/{home}/{away}` 端点 | +50 |
| `app/services/glicko2.py` | `lookup_glicko2_rating()` 加 1 行大写 key 快速路径(见下) | +5 |
| `tests/test_model_blend.py` | 新增 11 个 unit test(8 函数层 + 3 端点层) | +150 |

**注:1 个超出 v0.7.0a 范围但必须修的 bug**

### 🐛 Bug 修复(v0.6.0 latent): `glicko2.py` lookup 大写 key 缺失

**症状**:`lookup_glicko2_rating("USA")` 返回 None,但 `lookup_glicko2_rating("BRA")` 工作。

**根因**:
```
ratings.json 中:    'USA' (大写,key)
G2_NAME_TO_CODE 查: 'United States' (期望)
G2_FIFA_TO_SLUG 查: 'usa' (期望)
```

ratings 文件实际只存大写 key,`lookup_glicko2_rating` 永远查不到。

**影响**:
- v0.6.0 的 `/api/elo/predict-glicko2/USA/XXX` 端点全 404
- 2026 世界杯 32 强中至少 USA 不在 Glicko-2 端点可用范围
- 端点 80 队中估计还有 5-10 队同等问题

**修复**(5 行):
```python
def lookup_glicko2_rating(fifa_code: str) -> Optional[Dict]:
    data = load_glicko2_ratings()
    ratings = data.get("ratings", {})
    # v0.7.0a fix: ratings 文件中也可能用大写 FIFA code 作为 key(实测: 'USA' 大写存在)
    if fifa_code.upper() in ratings:
        return ratings[fifa_code.upper()]
    # ... 原逻辑保留
```

**为什么 v0.7.0a 修**:
- v0.7.0a 的**真实业务需求**是"USA 这种 2026 东道主必须能 blend"
- 这是 v0.7.0a 的**前置依赖**,不是 scope creep
- 按 scope discipline 严格说应独立 v0.6.1 patch,但 1 行 + 0 回归风险更稳

---

## 🆕 新增 API

```
GET /api/elo/predict-blend/{home_code}/{away_code}
  ?match_id=<int>           # 可选,自动写 prediction_log (model='v7a_blend')
  ?w_elo=<float 0-1>        # Elo 权重,默认 0.5
  ?w_glicko2=<float 0-1>    # Glicko-2 权重,默认 0.5
```

**响应**:
```json
{
  "home": {"fifa_code": "BRA", "elo": 1842, "rating_source": "hicruben"},
  "away": {"fifa_code": "ARG", "elo": 1810, "rating_source": "hicruben"},
  "elo": {
    "model": "elo_dixon_coles_v1",
    "probabilities": {"home_win": 0.45, "draw": 0.27, "away_win": 0.28},
    "data_as_of": "2026-06-11T..."
  },
  "glicko2": {
    "model": "glicko2_v1",
    "home": {"fifa_code": "BRA", "rating": 1789, "rd": 65, "volatility": 0.06},
    "away": {"fifa_code": "ARG", "rating": 1820, "rd": 60, "volatility": 0.06},
    "probabilities": {"home_win": 0.48, "draw": 0.25, "away_win": 0.27},
    "expected_score": 0.61,
    "uncertainty": 0.12,
    "data_source": "hicruben/world-cup-2026-prediction-model (913 matches walk-forward)",
    "data_as_of": "2026-06-11T..."
  },
  "blended": {
    "model": "blend_elo_glicko2_v1",
    "weights": {"elo": 0.5, "glicko2": 0.5},
    "probabilities": {"home_win": 0.465, "draw": 0.26, "away_win": 0.275}
  },
  "predicted_outcome": "H",
  "confidence": 0.465,
  "data_source": "hicruben/world-cup-2026-prediction-model",
  "data_as_of": "2026-06-11T...",
  "model_version": "v7a_blend",
  "error": null
}
```

## 🧪 测试覆盖(11 case)

| Case | 验证点 |
|---|---|
| `test_blend_basic_returns_valid_structure` | 5 字段齐全 + predicted_outcome 合法 |
| `test_blend_probs_sum_to_one` | 4 组对(BRA/ARG,GER/JPN,USA/MEX,ENG/FRA)三套概率加和 = 1.0 |
| `test_blend_w_elo_1_equals_pure_elo` | w_elo=1.0 时 blend = Elo |
| `test_blend_w_glicko2_1_equals_pure_glicko2` | w_glicko2=1.0 时 blend = Glicko-2 |
| `test_blend_unknown_team_returns_error` | 未知 FIFA code → error 字段非空 |
| `test_blend_predicted_outcome_logic` | argmax(blended_prob) = predicted_outcome |
| `test_blend_data_source_and_as_of` | data_source 含 hicruben,data_as_of 非空 |
| `test_blend_weights_in_response` | 权重参数正确反映在响应里 |
| `test_endpoint_predict_blend_returns_200` | 端点基本契约 |
| `test_endpoint_predict_blend_404_for_unknown` | 未知球队 404 |
| `test_endpoint_predict_blend_422_for_bad_weights` | 权重和不=1 → 422 |

## ⏭️ v0.7.0a 不做(留给后续)

- ❌ 前端 UI(等主人确认 JSON 满意)
- ❌ Alembic 迁移(无新表)
- ❌ 权重训练(无市场数据,等权 0.5/0.5)
- ❌ Monte Carlo(那是 v0.7.1)

## 🔜 路线图(主人原确认选项 1)

| 版本 | 范围 | 状态 |
|---|---|---|
| **v0.7.0a** ModelBlend | Elo + Glicko-2 等权 | ✅ 完成 |
| v0.7.1 Monte Carlo | 大赛 1000 次模拟 | ⏭️ 下一步 |
| v0.7.2 赔率 API 集成 | Pinnacle/The Odds API | ⏭️ |
| v0.7.3 真 MarketBlend | 三方加权 + 训练 | ⏭️ 最后 |

---

## ✅ 真实生产验证(2026-06-16 09:00 部署后)

### E2E 5 场景全 PASS

| 场景 | 请求 | 响应关键值 | 状态 |
|---|---|---|---|
| **T1** 等权 | `GET /api/elo/predict-blend/BRA/ARG` | `blended.home_win=0.3948` (= 0.5×0.3940 + 0.5×0.3956 ✅) | 200 |
| **T2** w_elo=0.7 | `GET /api/elo/predict-blend/BRA/ARG?w_elo=0.7&w_glicko2=0.3` | `blended.home_win=0.3945` (= 0.7×0.3940 + 0.3×0.3956 ✅) | 200 |
| **T3** 大写 fallback | `GET /api/elo/predict-blend/usa/mex` | `USA.elo=1826, MEX.elo=1826, blended.probs={H:0.38, D:0.2653, A:0.3548}` | 200 |
| **T4** 错误权重 | `?w_elo=0.3&w_glicko2=0.3` | `detail="w_elo + w_glicko2 必须等于 1.0"` | 422 ✅ |
| **T5** 未知球队 | `XXX/YYY` | `detail="球队 XXX 或 YYY 不在 Elo 数据中"` | 404 ✅ |

### 单元/集成测试 11/11 PASS (12.35s)

```
tests/test_model_blend.py::test_blend_basic_returns_valid_structure             PASSED
tests/test_model_blend.py::test_blend_probs_sum_to_one                          PASSED
tests/test_model_blend.py::test_blend_w_elo_1_equals_pure_elo                   PASSED
tests/test_model_blend.py::test_blend_w_glicko2_1_equals_pure_glicko2           PASSED
tests/test_model_blend.py::test_blend_unknown_team_returns_error                PASSED
tests/test_model_blend.py::test_blend_predicted_outcome_logic                   PASSED
tests/test_model_blend.py::test_blend_data_source_and_as_of                     PASSED
tests/test_model_blend.py::test_blend_weights_in_response                       PASSED
tests/test_model_blend.py::test_endpoint_predict_blend_returns_200              PASSED
tests/test_model_blend.py::test_endpoint_predict_blend_404_for_unknown          PASSED
tests/test_model_blend.py::test_endpoint_predict_blend_422_for_bad_weights      PASSED
============================= 11 passed in 12.35s =============================
```

### 数学正确性验证

```python
# 已知: elo.home_win=0.3940, glicko2.home_win=0.3956
w_elo=0.5:  0.5*0.3940 + 0.5*0.3956 = 0.3948 ✓
w_elo=0.7:  0.7*0.3940 + 0.3*0.3956 = 0.3945 ✓
# 两组算术完全一致,无浮点累积误差
```

### Git 证据

- commit: `844190a feat(elo): v0.7.0a ModelBlend (Elo + Glicko-2 weighted average)`
- tag: `v0.7.0a`
- 6 files changed, 553 insertions(+)
- 3 modified + 3 new: app/services/elo.py, app/routers/elo.py, app/services/glicko2.py, tests/test_model_blend.py, deliverables/v0.7.0a_{spec,release}.md

### 🚨 Scope 真实状态

- ✅ 端点 + 11 测试 + 数学正确 + 真实部署验证
- ❌ **未做**前端 UI(等主人确认 JSON 满意再接)
- ❌ **未做**prediction_log 写库(端点带 `match_id` 参数支持,但本验证未触发,需补 e2e 触发一条)
- ⏭️ Monte Carlo / 赔率 API / 真 MarketBlend 全在后续版本

### 💡 后续待办

- [ ] v0.7.0a-b: 补 1 个 e2e: `?match_id=1` 触发后查 `prediction_log` 表确认写入
- [x] v0.7.0a-c: 跑全量回归测试(280 passed + 1 skipped,259.50s,2026-06-16 11:36)✅
- [ ] v0.7.1: Monte Carlo 整届 10000 次模拟
