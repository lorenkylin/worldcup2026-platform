# v0.12.1 — CI YAML 稳定性修复

> 2026-06-17 — 距世界杯开赛 17 天

## 1. 一句话总结

> **修复 `.github/workflows/ci.yml` 中文半角冒号导致的 YAML 解析失败，并新增 2 个“真解析”回归测试，确保 ci.yml 与 docker-compose.yml 永远可加载。**

## 2. 改动清单

### 配置文件
- `.github/workflows/ci.yml`
  - `干跑:` → `干跑：`（全角冒号），消除 YAML key-value 歧义。
  - 统一中文注释标点，避免类似问题复发。

### 测试
- `tests/test_v012_deployment.py`
  - 新增 `test_ci_workflow_yaml_parses`
  - 新增 `test_compose_yaml_parses`
  - 断言顶层结构、jobs、触发器等关键字段。

## 3. 实测结果

| 命令 | 结果 |
|---|---|
| `pytest tests/test_v012_deployment.py -q` | **34 passed** |
| `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` | 无异常 |
| `python -c "import yaml; yaml.safe_load(open('docker-compose.yml'))"` | 无异常 |

## 4. 影响范围

- 仅 CI 配置与部署测试，无 API/前端/数据模型改动。
