# v0.16.3 — 推荐比分 UI 醒目化

> 2026-06-24 — 距世界杯开赛 11 天

## 1. 一句话总结

> **推荐比分视觉升级：首页焦点战、比赛详情 AI 预测区、赛后复盘的首选比分使用 amber 高亮加粗，次选比分使用 emerald 区分，深色背景下更醒目。**

## 2. 改动清单

### 修改文件
- `app/main.py`：`APP_VERSION` 升至 `0.16.3`
- `app/static/js/app.js`：
  - 比赛详情页 AI 预测区标题：首选/次选比分改为带背景色的 amber/emerald badge
  - 首页焦点战卡片：首选比分 `text-amber-300 text-lg font-extrabold`，次选 `text-emerald-300 font-semibold`
  - 赛后复盘卡片：预测比分 `text-amber-300 font-mono text-base`，次选 `text-emerald-300 font-semibold`
- `app/static/index.html`：CSS/JS query string 升级到 `?v=0.16.3`
- `app/static/sw.js`：缓存版本升至 `wc2026-v7`
- `README.md`：banner、文档版本、API 速查、阶段描述、§10 变更日志同步到 v0.16.3

### 新增交付物
- `deliverables/v0.16.3_spec.md`
- `deliverables/v0.16.3_release.md`

## 3. 实测结果

### 3.1 `/health`

```json
{
  "status": "healthy",
  "version": "0.16.3",
  "db_row_counts": {
    "matches": 104,
    "teams": 48,
    "standings": 48,
    "prediction_log": 11,
    "odds_snapshots": 2691
  },
  "scheduler_running": true
}
```

### 3.2 测试基线

| 范围 | 结果 |
|---|---|
| 非 E2E | **545 passed, 1 skipped** |
| E2E | **56 passed, 0 failed** |

## 4. 部署说明

- 静态资源已加 `?v=0.16.3`，浏览器会重新加载 JS/CSS。
- Service Worker 缓存版本升至 `wc2026-v7`，旧缓存会在 activate 时自动清理。
- 后端无 schema 变更，无需额外 Alembic 迁移。

## 5. 后续建议

- 观察真实用户设备上的可读性，必要时微调 amber/emerald 的亮度。
- 继续补齐历史版本交付物：v0.14.x / v0.15.0 / v0.16.0。
