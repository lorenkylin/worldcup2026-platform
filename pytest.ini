[pytest]
markers =
    slow: tests that take a long time (e.g. walkforward, monte carlo simulations)
    e2e: end-to-end tests requiring a running server and Playwright browser
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
